pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        maven { url = 'https://maven.quiltmc.org/repository/release' }
        gradlePluginPortal()
    }
}

plugins {
    id "fabric-loom" version "0.12-SNAPSHOT"
}

rootProject.name = "speedrun-ranked-mod"
